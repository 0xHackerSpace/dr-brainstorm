# Example inventory mirroring `terraform output recovery_site_hosts`.
# Real environments should generate this file, or swap it for a dynamic
# inventory plugin, rather than hand-maintaining hostnames.

[primary]
primary-01 ansible_host=192.0.2.11

[recovery_control]
dr-brainstorm-example-recovery-01 ansible_host=198.51.100.11

[recovery_workload]
dr-brainstorm-example-recovery-02 ansible_host=198.51.100.12

[recovery:children]
recovery_control
recovery_workload

[dr_managed:children]
primary
recovery

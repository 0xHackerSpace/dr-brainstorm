# NNNN — Title

- **Status:** Draft
- **Date:** YYYY-MM-DD
- **Author:** —
- **Supersedes:** —
- **Superseded by:** —
- **RPO impact:** none | <n> minutes
- **RTO impact:** none | <n> minutes

## Context

What is true today, and what about it is a problem. Concrete: what breaks, what
is missing, what someone cannot currently do. Include the forces that make this
non-obvious — cost, complexity, blast radius, whatever they are.

No solutions in this section.

## Decision

What we are going to do, stated as a decision in the present tense. One
decision. If you need "and also", you probably need a second spec.

## Scope

What this spec covers. Be specific enough that a reviewer can tell whether a
given file belongs in the PR.

### Out of scope

What this spec deliberately does not cover, and — where useful — which future
spec is expected to pick it up. This is the section that prevents the argument
in review.

## Design

How it works. Interfaces, variable names, file layout, the shape of the data
that crosses each boundary. Include the reasoning for anything a reader would
otherwise want to change.

## Acceptance criteria

A checklist a reviewer can verify mechanically. Every item is either a command
that exits 0 or a file that exists with stated content. No adjectives.

- [ ] `<command>` exits 0
- [ ] `<path>` exists and contains `<thing>`

## Alternatives considered

At least two, each with the reason it lost. A spec with no rejected option was
not a decision, it was a description.

**<Alternative>.** What it was, and why not.

## Risks

What could go wrong with this decision, and what would tell us it has.

## Follow-up work

Known gaps this spec knowingly leaves open, in the order they should be picked
up. Link the specs once they exist.
